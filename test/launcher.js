/* eslint-env mocha */

describe('bash script', function () {
  it('attaches environment config file')
  describe('host environment vars', function () {
    it('passes $HOME')
    it('passes $EMPIRICAL_HOST')
    it('passes $DEBUG')
    it('passes $NODE_ENV')
  })
  describe('emp run', function () {
    it('attaches code directory as a volume')
  })
  describe('emp data', function () {
    it('attaches data file')
  })
})
